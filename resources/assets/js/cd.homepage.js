$.using("cd.homepage", function (context, $, W) {

    var NS = context._NS;

  $('.mycarousel').slick({
      dots: true,
      speed: 300,
      autoplay: true,
      cssEase: 'linear',
      autoplaySpeed: 5000,
      arrows: false,
  })




});
